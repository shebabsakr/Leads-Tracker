The Leads Tracker enables users to:
Enter a URL
Save it to a list
Display saved links dynamically
Open saved links in a new browser tab
This project focuses on practicing DOM manipulation and dynamic rendering in JavaScript.

🚀 Features
Add new leads (URLs)
Display links as clickable items
Open links in a new tab (target="_blank")
Dynamic rendering using template literals
Clean and minimal UI

🛠 Technologies Used
HTML5 – Structure
CSS3 – Styling
JavaScript (Vanilla) – Logic and DOM updates

⚙️ How It Works
User enters a URL in the input field.
Clicking the Save button pushes the value into an array.
The list is re-rendered using a loop and template literals.
Each saved URL appears as a clickable link.
